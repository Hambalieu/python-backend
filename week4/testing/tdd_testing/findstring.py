#checks for presents of name in the list
def is_present(person):
  names = ["Al", "Bo", "Chi", "Ma"]
  if person in names:
    return True
  else:
    return False

#checks that the name does not contain digits
def nodigit():
  if person.isalpha():
    return True
  else:
    return False
